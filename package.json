{
  "name": "detector-citas-madrid",
  "version": "0.5.0",
  "private": true,
  "description": "Detector de Citas para NIE/TIE en Madrid con SMS, monitorizacion por tramite, perfil cifrado y panel admin",
  "type": "module",
  "scripts": {
    "start": "node --import ./runtime-upgrade.js gateway.js",
    "check:tie": "node scripts/check-tie.js",
    "check": "node --check runtime-upgrade.js && node --check gateway.js && node --check profile-ui.js && node --check server.js && node --check src/icpplus.js && node --check scripts/check-tie.js && node --check sw.js"
  },
  "dependencies": {
    "playwright": "^1.55.0",
    "web-push": "^3.6.7"
  }
}
