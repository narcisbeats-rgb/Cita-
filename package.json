{
  "name": "citanie-madrid",
  "version": "0.4.0",
  "private": true,
  "description": "MVP de monitorización segura de disponibilidad NIE/TIE en Madrid con alertas push PWA y perfil cifrado",
  "type": "module",
  "scripts": {
    "start": "node gateway.js",
    "check:tie": "node scripts/check-tie.js",
    "check": "node --check gateway.js && node --check profile-ui.js && node --check server.js && node --check src/icpplus.js && node --check scripts/check-tie.js && node --check sw.js"
  },
  "dependencies": {
    "playwright": "^1.55.0",
    "web-push": "^3.6.7"
  }
}
